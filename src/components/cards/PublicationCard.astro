---
import Card from "../Card.astro";
const { entry } = Astro.props;

const href = entry.data?.link ?? null;
const action = href ? { type: "link", href } : null;
---

<Card entry={entry} action={action} />
