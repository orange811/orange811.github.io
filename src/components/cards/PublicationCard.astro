---
import Card from "../Card.astro";
const { entry, clickable = true } = Astro.props;

// Default to modal so clicking opens the abstract; external link is shown inside the modal
const action = { type: "modal" as const };
---

<Card entry={entry} action={action} clickable={clickable} />
